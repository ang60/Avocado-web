# Copy to `.env.production` then: npm run build
# Or set VITE_API_BASE_URL in your static host / Vercel project settings.

# Django API (same as Android Constants.BASE_URL):
VITE_API_BASE_URL=https://avo-guard.vercel.app

# Do NOT use the SPA-only host unless Apache/nginx proxies /api to Django:
# VITE_API_BASE_URL=https://avoguard.cognitron.co.ke

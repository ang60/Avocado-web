# Baked in by: npm run build
# Must be the Django API — NOT the static dashboard host unless /api is proxied there.

VITE_API_BASE_URL=https://avo-guard.vercel.app

# Wrong for API calls (SPA only — POST /api/... → "Method POST not allowed"):
# VITE_API_BASE_URL=https://avoguard.cognitron.co.ke

# Production build for https://avoguard.cognitron.co.ke
# API calls go to the same host (/api/...); Apache must proxy to Django (see deploy/apache-avoguard-api-proxy.conf).
VITE_API_BASE_URL=https://avoguard.cognitron.co.ke

# Dependencies
node_modules/

# Build / cache
# dist/ is tracked so you can push built files for simple static hosting (git pull + serve).
# If you prefer CI-only builds, uncomment the next line and stop committing dist/.
# dist/
*.tsbuildinfo
.vite/

# Env & local
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/

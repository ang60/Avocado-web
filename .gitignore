# Dependencies
node_modules/

# Build / cache
# dist/ is tracked so you can push built files for simple static hosting (git pull + serve).
# If you prefer CI-only builds, uncomment the next line and stop committing dist/.
# dist/
*.tsbuildinfo
.vite/

# Django backend (local DB / venv)
backend/db.sqlite3
backend/.venv/
backend/**/__pycache__/

# Env & local
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/

# Markdown docs (keep repo readme only)
*.md
!README.md
!readme.md

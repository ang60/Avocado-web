# Vite frontend — place in repo root (same folder as vite.config.ts).
# Local dev: copy to `.env.local`
# Production build: use `.env.production` (see `.env.production.example`)
# Default if unset: http://localhost:8000
VITE_API_BASE_URL=http://127.0.0.1:8000
